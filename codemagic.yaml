workflows:
  sample-workflow:
    name: ToolNest workflow
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: tool_nest
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.toolsforappcomp.ToolNest
      vars:
        APP_STORE_CONNECT_KEY: tool_nest
        APP_STORE_APPLE_ID: 6778512679
        BUNDLE_ID: "com.toolsforappcomp.ToolNest"
        XCODE_PROJECT: "ToolNest.xcodeproj"
        XCODE_SCHEME: "ToolNest"
    scripts:
      - name: Clean and Resolve SPM Dependencies
        script: |
          # Очистка для избежания проблем с кэшем
          rm -rf ~/Library/Developer/Xcode/DerivedData
          rm -rf .build
          xcodebuild clean -project "$CM_BUILD_DIR/$XCODE_PROJECT" -scheme "$XCODE_SCHEME"
          xcodebuild -resolvePackageDependencies -project "$CM_BUILD_DIR/$XCODE_PROJECT" -scheme "$XCODE_SCHEME"
      - name: Set up provisioning profiles settings on Xcode project
        script: xcode-project use-profiles
      - name: Increment build number
        script: | 
          #!/bin/sh
          cd $CM_BUILD_DIR
          LATEST_BUILD_NUMBER=$(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID")
          agvtool new-version -all 1
      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_KEY
        auth: integration
        submit_to_app_store: true
        cancel_previous_submissions: true
        release_type: SCHEDULED
